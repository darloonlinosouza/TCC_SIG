pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            // url da api do arcGis
            url 'https://esri.jfrog.io/artifactory/arcgis'
        }
    }
}
rootProject.name = "TCC_SIG"
include ':app'
